public static void main(String [] args){
    double [][] matriz = {{a00, a01,a02},{a10, a11, a12}}
    bjMatriz.metodoMatriz(matriz);
}

public double metodoMatriz (double [][] m){
    // A nivel de toda la matriz
    for (int fila = 0; fila < m.length; fila++){ //filas
        //recorrer cada fila

        for (int col = 0; col < m[fila].length; col++){//columnas
            //recorrer todos los elementos de cada fila
        }
        //Recorrer cada fila
    }
    return = 0
}

public int encontrarModa(int[][] temp){
    for (int fila = 0; fila < temp.length; fila++){
        //columnas
        for (int columnas = 0; columnas < temp[fila].length; col++){
            
        }
    }
}